/**
 * Tag to denote current version (alpha) of Asterspark next to the logo
 */

import React from "react";

function AlphaTag(): JSX.Element {
  return (
    <div className="px-2 text-xs font-semibold bg-blue-100 text-blue-800 text-center rounded-full w-auto">
      Alpha
    </div>
  );
}

export default AlphaTag;
